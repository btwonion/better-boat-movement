modLoader = "klf"
loaderVersion = "[1,)"
license = "${mod_license}"

issueTrackerURL = "https://github.com/${repo}/issues"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
authors = "${mod_author}"
description = '''
${mod_description}
'''
logoFile = "${icon}"

[[mixins]]
config = "bbm.mixins.json"

[[dependencies.${mod_id}]]
modId = "minecraft"
versionRange = "${mc}"
ordering = "NONE"
side = "BOTH"
